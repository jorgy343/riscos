[build]
target = "riscv64gc-unknown-none-elf"

[target.riscv64gc-unknown-none-elf]
rustflags = ["-C", "link-arg=-Tlinker.ld", "-C", "relocation-model=pic", "--emit=asm"]

[check]
allTargets = false

[unstable]
profile-rustflags = true